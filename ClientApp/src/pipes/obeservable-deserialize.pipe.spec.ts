import { ObeservableDeserializePipe } from './obeservable-deserialize.pipe';

describe('ObeservableDeserializePipe', () => {
  it('create an instance', () => {
    const pipe = new ObeservableDeserializePipe();
    expect(pipe).toBeTruthy();
  });
});
